# Mode A: Let's Encrypt (DNS points straight at EC2; certbot adds the 443 block automatically)
server {
    listen 80;
    listen [::]:80;
    server_name __DOMAIN__;
    include /etc/nginx/snippets/toddler-music-box-app.conf;
}
